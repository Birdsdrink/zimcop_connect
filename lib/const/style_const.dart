import 'package:flutter/material.dart';

const backgroundColor = Colors.amber;
const homeBackgroundColor = Colors.black;
const tittleTextColor = Colors.white70;
const subTittleTextColor = Colors.white38;
final containerColor = Colors.black.withOpacity(0.8);
const appBarColor = Colors.black;
const zrpBlue = Colors.blueAccent;
const appPadding = EdgeInsets.only(top: 40, bottom: 15, left: 10, right: 10);
const greyColor = Colors.grey;
